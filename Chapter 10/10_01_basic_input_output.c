#include <stdio.h>

int main(){
    FILE *ptr;
    // ptr = fopen("sample.txt","r"); //--> this is for opening the file
    // ptr = fopen("sample.txt","w"); //--> this is for writing the file
    ptr = fopen("sample2.txt","w"); //--> this is for generating and writing the file
     return 0;
}