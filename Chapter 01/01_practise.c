#include <stdio.h>

int main()
{
    int length, breadth;

    printf("What is the Value length :\n");
    scanf("%d", &length);

    printf("What is the Value breadth :\n");
    scanf("%d", &breadth);

    printf("The area of rectangle is %d", length*breadth);
    return 0;
}
